// Editable Wrapper provide an easy way to give your plugins customizability. 
// This wrapper replaces the Label text in an input with an editable textbox that saves on Enter.
export const EditableInputWrapper: React.FC<React.PropsWithChildren> = ({ children }) => {
    return <>children</>;
}
