import { useContext } from "react"
import { VarPluginContext } from "../PluginManager/Loaders/VarPluginProvider";

export const DraggableVariableList = ({ }) => {
    const VarPlugins = useContext(VarPluginContext);

    return 
}